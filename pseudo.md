// Calculer le temps pour traverser la route
double temps_route = 6 / 5

// Retrouver la distance sur la terrain rocheux
double distance_rocheux = sqrt(3^2 + (10 - 6)^2)

// Calculer le temps pour traverser la terrain rocheux
double temps_rocheux = distance_rocheux / 2

// Calculer le temps totale
double temps_totale = temps_rocheux + temps_route

Bonus:

double L1 = user input

...

// Calculer la distance qui permettra de récupérer l'objet le plus rapidement possible.
double distance_rocheux = sqrt(pow(3.0, 2.0) + pow(abs(10 - L1),2.0))

...
